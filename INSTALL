
Requirements:

* qt5-core, qt5-widgets, qt5-xml and qt5-qmake

To install just follow:

$ qmake
$ make

Obs:
"make install" is not suported yet, so, your binary file will be generated in
the current dir.

To run invoke:

$ ./simvida
